(function(){
    "use strict";

    var confObj = {
        thWidth: 150,
        thHeight: 150,
        dropbox: '.dropbox',
        inputImg: '.inputImg',
        allowedFilesType: /^image\//
    };
    imageProcessor.init(confObj);

})();